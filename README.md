# 🎫 سیستم مدیریت تیکت پشتیبانی
## بازطراحی با الگوهای طراحی (State · Strategy · Factory)

## بخش ۱ — تشخیص زیرمسائل و الگوها

### زیرمسئله ۱: رفتار وابسته به حالت (State-Dependent Behavior)
**توضیح:** در کد اولیه، متد `handle()` با یک زنجیره بلند از `if/else` وضعیت تیکت را بررسی می‌کند (`NEW`، `ASSIGNED`، `IN_PROGRESS`، `RESOLVED`، `CLOSED`). رفتار سیستم در هر وضعیت کاملاً متفاوت است.

**الگوی پیشنهادی: State Pattern**

**دلیل:** هر وضعیت رفتار مجزایی دارد. با State Pattern هر وضعیت در یک کلاس جداگانه캡سوله می‌شود و انتقال بین وضعیت‌ها درون خود آن کلاس انجام می‌گیرد.

**روش اعمال:**
- کلاس پایه‌ی انتزاعی `TicketState` با متد `handle(ticket, service)` تعریف می‌شود.
- پنج کلاس مشخص: `NewState`، `AssignedState`، `InProgressState`، `ResolvedState`، `ClosedState` ساخته می‌شود.
- هر کلاس پس از اجرای رفتار خود، وضعیت تیکت را به وضعیت بعدی تغییر می‌دهد.

---

### زیرمسئله ۲: رفتارهایی که به چند روش انجام می‌شوند (Algorithmic Variation)
**توضیح:** دو رفتار در سیستم بسته به نوع تیکت (`BUG` یا `SUPPORT`) متفاوت عمل می‌کنند:
- **ارجاع:** باگ → تیم مهندسی، پشتیبانی → تیم پشتیبانی
- **پاسخ‌دهی:** باگ → پاسخ فنی، پشتیبانی → پاسخ عمومی

**الگوی پیشنهادی: Strategy Pattern**

**دلیل:** هر دو رفتار می‌توانند به روش‌های مختلف پیاده‌سازی شوند. با Strategy می‌توان الگوریتم را از کلاس استفاده‌کننده جدا کرد و به‌راحتی نوع جدیدی اضافه نمود.

**روش اعمال:**
- دو interface انتزاعی: `AssignmentStrategy` و `ResponseStrategy`
- پیاده‌سازی‌های مشخص: `BugAssignmentStrategy`، `SupportAssignmentStrategy`، `BugResponseStrategy`، `GenericResponseStrategy`
- استراتژی‌ها از بیرون به `TicketService` تزریق می‌شوند.

---

### زیرمسئله ۳: ساخت اشیاء پیچیده (Object Creation)
**توضیح:** ایجاد یک تیکت نیاز دارد که همزمان شیء `Ticket`، استراتژی ارجاع و استراتژی پاسخ متناسب با آن ساخته شوند. این منطق نباید در کد اصلی (main) پراکنده باشد.

**الگوی پیشنهادی: Factory Pattern**

**دلیل:** ساخت اشیاء وابسته به نوع تیکت است. Factory این وابستگی را캡سوله می‌کند و کد main را ساده نگه می‌دارد.

**روش اعمال:**
- کلاس `TicketFactory` با متد استاتیک `create(id, channel, type)` تعریف می‌شود.
- این متد شیء `Ticket` به همراه استراتژی‌های مناسب آن را برمی‌گرداند.


---

## بخش ۲ — Class Diagram

<img width="2811" height="794" alt="UML-Ticket" src="https://github.com/user-attachments/assets/845f3629-ab46-490f-b9cf-ed2a8bad6a98" />

